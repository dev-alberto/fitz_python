from strategy.alpha import Alpha
{featureImport}

class {alphaName}(Alpha):

    def __init__(self, {featureList}):

        {featureInit}

        super().__init__(feature_list=[{featureList}])

    def compute(self, ii):
        pass
